# Exercises

 1. What is the distribution of the wavelel power of white noise? Assume
    that the wavelet was L2-normalized.
 2. postdoc Ole computes L2-CWT of signal composed of white noise and 
    oscillation. Ole wants a significance criterion for the wavelet power 
    associated with the oscillation. Ole has already used a threshold, 
    defined as 3 times the wavelet noise floor, which was estimated as the
    mean of the wavelet power in frequency band containing no oscillations,
    only white noise. Ole heard that a propper way is to compute Monte-Carlo
    confidence limits. Ole could do that by estimating the white noise by
    filtering the signal. Should Ole reanalyze the data this way? What is
    the relationship between the threshold Ole computed and a confidence 
    limit?